<div class="modal-footer">
    {{-- @if (!$toggleForm) --}}
        <x-submit-button class="btn-success">{{ __('Save') }}</x-submit-button>
    {{-- @else
        <x-button class="btn-success">{{ __('Update') }}</x-button>
    @endif --}}
</div>